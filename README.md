# GitHub Pages Deployment

This project demonstrates deploying a static website to GitHub Pages using GitHub Actions.  
Whenever the `index.html` file is changed and pushed to `main`, GitHub Actions automatically deploys it to GitHub Pages.
